# Configuraci�n para PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/megastore_db
spring.datasource.username=postgres
spring.datasource.password=123456.a.44474316
spring.datasource.driver-class-name=org.postgresql.Driver

# Dialecto de Hibernate para PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuraci�n de JPA e Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Opcional: Desactivar banner de Spring Boot
spring.main.banner-mode=off
